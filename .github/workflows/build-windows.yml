name: Build Windows

on:
  workflow_dispatch:
    inputs:
      config:
        description: 'Build configuration'
        required: false
        default: 'Release'
        type: choice
        options:
          - Release
          - Debug

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install CMake
        uses: lukka/get-cmake@latest

      - name: Configure
        run: |
          cmake -S . -B BUILD -DCMAKE_BUILD_TYPE=${{ github.event.inputs.config || 'Release' }}

      - name: Build VST3
        run: |
          cmake --build BUILD --target Rompler_VST3 --config ${{ github.event.inputs.config || 'Release' }}

      - name: Build Standalone
        run: |
          cmake --build BUILD --target Rompler_Standalone --config ${{ github.event.inputs.config || 'Release' }}

      - name: Build All
        run: |
          cmake --build BUILD --target Rompler_All --config ${{ github.event.inputs.config || 'Release' }}

      - name: Package bundles
        shell: pwsh
        run: |
          $config = "${{ github.event.inputs.config || 'Release' }}"
          Compress-Archive -Path "BUILD/Rompler_artefacts/$config/VST3/Rompler.vst3" -DestinationPath "Rompler-Windows-VST3.zip"
          Compress-Archive -Path "BUILD/Rompler_artefacts/$config/Standalone/Rompler.exe" -DestinationPath "Rompler-Windows-Standalone.zip"

      - name: Upload VST3
        uses: actions/upload-artifact@v4
        with:
          name: Rompler-Windows-VST3
          path: Rompler-Windows-VST3.zip

      - name: Upload Standalone
        uses: actions/upload-artifact@v4
        with:
          name: Rompler-Windows-Standalone
          path: Rompler-Windows-Standalone.zip
